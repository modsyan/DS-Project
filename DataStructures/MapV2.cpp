#include <iostream>
#include "LinkedList.cpp"
using namespace std;

template <class k, class v> struct pairs {
    k key;
    v value;
    pairs(k key, v value) {
        key = key;
        value = value;
    }
};

class pairs

int main() {
    LinkedList<pairs<string, int>> map;
    pairs<pai
    map.pushBack();
}